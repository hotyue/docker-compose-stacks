# Docker Compose Stacks
# GOVERNANCE.md

## 项目治理规范（v1.1.x 冻结基线）

> 本文档用于固化 Docker Compose Stacks 项目在 v1.1.x 系列中形成并经验证的治理规范。
>
> 本规范来源于已冻结的实现事实，而非设计假设，适用于后续所有版本（除非被显式废止）。

---

## 一、治理目标

Docker Compose Stacks 项目的治理目标是：

**将一组可运行的 Docker Compose Stack，治理为一套可自治、可审计、可演进的 Stack 体系。**

治理优先级高于功能丰富度，任何实现必须首先满足治理规范，方可被视为合格。

---

## 二、核心治理原则（冻结）

### 原则一｜Stack 必须具备自治能力（Autonomy）

每一个 Stack 必须在以下前提下完成自身运行所需的全部前置条件：

- 不依赖人工手工步骤
- 不依赖 Installer 兜底逻辑
- 不修改平台级资源（如数据库 Stack）

依赖“请先手工准备环境”的 Stack，不被视为合格实现。

---

### 原则二｜平台级资源 ≠ 平台级责任

平台级资源（如 MariaDB、Redis、网络）仅提供**能力**，不承担任何下游 Stack 的业务或初始化责任。

- 平台资源不得感知下游应用的存在
- 应用 Stack 必须自行完成其数据库、账号、权限的治理

---

### 原则三｜初始化阶段 ≠ 运行阶段（权限分离）

初始化阶段与运行阶段在治理上被严格区分：

- 初始化阶段：一次性治理行为
- 运行阶段：长期最小权限运行

治理要求：

- 初始化账号与运行期账号必须分离
- 初始化服务成功后必须退出
- 运行期禁止使用 root / 管理账号

---

### 原则四｜数据库变量是资源语义，而非实现细节

数据库相关变量必须反映治理语义：

- `*_DB_HOST / *_DB_PORT`：数据库资源定位
- `*_DB_ADMIN_USER / PASSWORD`：初始化期权限
- `*_DB_USER / PASSWORD`：运行期最小权限

明确禁止：

- admin host / admin port 语义
- 初始化与运行期变量混用
- 在脚本中硬编码任何凭据

---

### 原则五｜幂等性是事实资格的前提

任何初始化或治理行为，必须满足幂等性要求：

- 可重复执行
- 不产生副作用
- 不依赖“第一次和以后不一样”的操作流程

不具备幂等性的实现，不得进入事实体系。

---

## 三、标准 Stack 结构模板（冻结）

所有合格 Stack 应遵循以下基础结构：

```
Stack/
├── docker-compose.yml
├── .env (.env.example)
├── init/
│   └── 10-init-<stack>-db.sh
└── README.md
```

### 各组件职责

| 组件 | 职责 |
|----|----|
| docker-compose.yml | 服务结构与依赖关系 |
| .env | 治理变量声明与配置边界 |
| init/ | 一次性自治初始化资产 |
| README.md | 事实描述（非承诺性文档） |

---

## 四、明确禁止的反模式（黑名单）

以下模式在本项目中被明确禁止：

- 依赖人工预创建数据库或账号
- 让 Installer 参与数据库或权限治理
- 让平台级数据库为应用兜底
- 使用单一高权限账号贯穿初始化与运行期
- 通过 README 提示人工补步骤

任何出现上述模式的实现，不得被合并或回写。

---

## 五、对后续版本的约束性影响

自 v1.1.x 起：

- 新增 Stack 必须直接遵循本治理规范
- 旧 Stack 的治理缺陷只能通过显式版本修复
- 不得回退到“能跑即可”的实现模式

---

## 六、规范冻结声明

本治理规范来源于 v1.1.x 系列已完成并冻结的实现事实。

在未被后续版本显式修订或废止前：

- 本规范持续生效
- 作为项目治理基线、审计依据与继承前提

---

**GOVERNANCE.md**

状态：Final · Frozen

