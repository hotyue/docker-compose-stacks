# Changelog

本文件记录 Docker Compose Stacks 项目的**已发布版本变更事实**。  
仅包含已经完成并冻结的实现，不记录计划或未发布内容。

---

## v1.1.12

发布日期：2026-01-16  
版本类型：功能扩展版本（非破坏性）

### 新增

- 新增 Vaultwarden / Server Stack，提供自托管的密码管理服务端能力：
  - 兼容 Bitwarden 客户端
  - 用于安全存储与管理密码、凭据及相关敏感信息
  - Stack 分类归属为 security

- Vaultwarden Stack 引入外置数据库治理机制：
  - 使用平台级 MariaDB 作为外置数据库
  - 通过一次性初始化容器完成：
    - Vaultwarden 专用逻辑数据库创建
    - Vaultwarden 专用数据库用户创建
    - 最小权限授权（仅限自身数据库）
  - 运行期服务仅使用专用数据库用户，不使用管理员账号

- Vaultwarden Stack 接入平台统一反向代理体系：
  - HTTPS 终止由 Nginx Proxy Manager 统一承担
  - 服务通过平台 proxy 网络接入反代
  - 不直接对外暴露 TLS

- Vaultwarden 默认启用 WebSocket 通知通道：
  - 与 HTTP API 同端口、同进程
  - 用于提升实时同步与通知体验
  - 支持 Docker Compose + NPM 反代部署
  - 支持 Cloudflare 橙云 / Zero Trust Tunnel 场景

### 改进

- 明确并实践“端口最小侵入原则”：
  - Vaultwarden 容器内部监听端口保持官方默认（80）
  - 平台不通过环境变量覆盖应用原生监听行为
  - 宿主机端口（如 8038）仅作为可选的 IP 直连调试映射
  - 反代配置统一使用容器名 + 内部端口

- Vaultwarden Stack 配置治理规范化：
  - 提供完整 `.env.example`
  - 所有关键参数通过环境变量显式声明
  - 明确区分内部端口语义与宿主机端口映射语义

### 行为约束

- Vaultwarden 运行期禁止使用数据库管理员账号
- 数据库初始化逻辑遵循最小权限原则，适配 MariaDB 11 语义
- 初始化容器仅负责创建与授权，不参与运行期逻辑
- WebSocket 作为体验增强能力：
  - 不作为核心功能可用性的前置条件
  - 不纳入平台稳定性或 SLA

### 不影响范围

- 平台级 MariaDB Stack 的初始化与运行逻辑
- Nginx Proxy Manager 的既有部署与行为
- 其他已存在 Stack 的运行方式与冻结事实
- v1.1.11 及之前版本的全部冻结内容

---

## v1.1.7

发布日期：2026-01-14  
版本类型：治理演进版本（非破坏性）

### 新增

- Matomo Stack 引入一次性数据库初始化机制，在首次启动阶段自动完成：
  - Matomo 专属逻辑数据库创建
  - Matomo 专属数据库用户创建
  - 最小权限授权（仅限自身数据库）

- Matomo Stack 明确区分数据库账号语义：
  - 初始化阶段账号仅用于创建与授权
  - 运行期服务仅使用最小权限账号

### 改进

- Installer 在首次安装 Stack 时，确保 Stack 私有资产被完整投放至运行目录  
  （包括初始化脚本目录等私有资源）

- 安装流程在首次生成 `.env` 后采用受控中断机制：
  - 强制用户完成配置项确认
  - 在配置未完成前，不启动服务、不执行初始化逻辑

### 行为约束

- Matomo 运行期不使用 root、db_admin 或任何平台级数据库账号
- 数据库初始化逻辑遵循最小授权原则，适配 MariaDB 11 语义
- 初始化逻辑具备幂等性，可安全重复执行

### 不影响范围

- MariaDB Stack 的初始化与运行逻辑
- 其他已存在 Stack 的行为
- v1.1.6 及之前版本的冻结事实

---
