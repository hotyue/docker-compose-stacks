# Changelog

本文件记录 Docker Compose Stacks 项目的**已发布版本变更事实**。  
仅包含已经完成并冻结的实现，不记录计划、假设或未发布内容。

---

## v1.1.12

发布日期：2026-01-16  
版本类型：功能扩展版本（非破坏性）

### 新增

- 新增 Vaultwarden / Server Stack，提供自托管的密码管理服务端能力：
  - 兼容 Bitwarden 客户端
  - 用于安全存储与管理密码、凭据及相关敏感信息
  - Stack 分类归属为 `security`

- Vaultwarden Stack 引入外置数据库治理机制：
  - 使用平台级 MariaDB 作为外置数据库
  - 通过一次性初始化容器完成：
    - Vaultwarden 专用逻辑数据库创建
    - Vaultwarden 专用数据库用户创建
    - 最小权限授权（仅限自身数据库）
  - 运行期服务仅使用专用数据库用户，不使用管理员账号

- Vaultwarden Stack 接入平台统一反向代理体系：
  - HTTPS 终止由 Nginx Proxy Manager 统一承担
  - 服务通过平台 `proxy` 网络接入反代
  - 不直接对外暴露 TLS

- Vaultwarden 默认启用 WebSocket 通知通道：
  - 与 HTTP API 同端口、同进程
  - 用于提升实时同步与通知体验
  - 支持 Docker Compose + NPM 反代部署
  - 支持 Cloudflare 橙云 / Zero Trust Tunnel 场景

### 改进

- 明确并实践“端口最小侵入原则”：
  - Vaultwarden 容器内部监听端口保持官方默认（80）
  - 平台不通过环境变量覆盖应用原生监听行为
  - 宿主机端口仅作为可选的 IP 直连调试映射
  - 反代配置统一使用容器名 + 内部端口

- Vaultwarden Stack 配置治理规范化：
  - 提供完整 `.env.example`
  - 所有关键参数通过环境变量显式声明
  - 明确区分内部端口语义与宿主机端口映射语义

### 行为约束

- Vaultwarden 运行期禁止使用数据库管理员账号
- 数据库初始化逻辑遵循最小权限原则，适配 MariaDB 11 语义
- 初始化容器仅负责创建与授权，不参与运行期逻辑
- WebSocket 作为体验增强能力：
  - 不作为核心功能可用性的前置条件
  - 不纳入平台稳定性或 SLA

### 不影响范围

- 平台级 MariaDB Stack 的初始化与运行逻辑
- Nginx Proxy Manager 的既有部署与行为
- 其他已存在 Stack 的运行方式与冻结事实
- v1.1.11 及之前版本的全部冻结内容

---

## v1.1.11

发布日期：2026-01-16  
版本类型：目标终止版本（无实现发布）

### 说明

- 本版本原计划在 Matomo 初始化阶段引入额外运行期配置能力
- 经实现阶段审查确认：
  - 与项目既有安装流程与治理模型不兼容
  - 不具备可验证、可回写的实现路径

### 裁定结果

- v1.1.11 功能目标被正式终止
- 本版本不产生任何新增实现事实
- 不进入项目事实体系
- 版本号保留，仅用于历史审计与治理追溯

---

## v1.1.10

发布日期：2026-01-14  
版本类型：功能扩展版本（非破坏性）

### 新增

- 新增 WordPress Stack：
  - 支持通过 Docker Compose 部署
  - 作为通用内容管理与博客系统使用

### 行为约束

- WordPress Stack 遵循平台既有数据库与反向代理治理规范
- 不引入平台级数据库模型变更

### 不影响范围

- 既有 Stack 的运行方式
- 平台数据库与网络治理模型
- v1.1.9 及之前版本的冻结事实

---

## v1.1.9

发布日期：2026-01-14  
版本类型：治理确认版本（非功能扩展）

### 结论性事实

- 对 Nezha Server Stack 的数据库分离能力进行验证
- 经部署与运行测试确认：
  - 当前 Nezha Server **不支持外置数据库运行**
  - 即使提供外置数据库配置，也不会被实际使用

### 裁定结果

- 明确并冻结以下结论：
  - Nezha Server 当前版本仅支持内置数据库模式
  - 不作为数据库分离治理的可用 Stack

### 不影响范围

- 其他 Stack 的数据库治理模式
- 平台级 MariaDB 的运行与初始化逻辑

---

## v1.1.8

发布日期：2026-01-14  
版本类型：功能演进版本（非破坏性）

### 改进

- 对 Nginx Proxy Manager Stack 进行数据库分离改造：
  - 引入外置 MariaDB 作为运行数据库
  - 数据库初始化通过一次性初始化机制完成
  - 明确数据库账号与权限边界

### 行为约束

- Nginx Proxy Manager 运行期不使用数据库管理员账号
- 数据库初始化逻辑遵循最小权限原则
- 初始化流程具备幂等性，可重复执行

### 不影响范围

- 反向代理功能本身
- 其他 Stack 的部署与运行方式

---

## v1.1.7

发布日期：2026-01-14  
版本类型：治理演进版本（非破坏性）

### 新增

- Matomo Stack 引入一次性数据库初始化机制，在首次启动阶段自动完成：
  - Matomo 专属逻辑数据库创建
  - Matomo 专属数据库用户创建
  - 最小权限授权（仅限自身数据库）

- Matomo Stack 明确区分数据库账号语义：
  - 初始化阶段账号仅用于创建与授权
  - 运行期服务仅使用最小权限账号

### 改进

- Installer 在首次安装 Stack 时，确保 Stack 私有资产被完整投放至运行目录  
  （包括初始化脚本目录等私有资源）

- 安装流程在首次生成 `.env` 后采用受控中断机制：
  - 强制用户完成配置项确认
  - 在配置未完成前，不启动服务、不执行初始化逻辑

### 行为约束

- Matomo 运行期不使用 root、db_admin 或任何平台级数据库账号
- 数据库初始化逻辑遵循最小授权原则，适配 MariaDB 11 语义
- 初始化逻辑具备幂等性，可安全重复执行

### 不影响范围

- MariaDB Stack 的初始化与运行逻辑
- 其他已存在 Stack 的行为
- v1.1.6 及之前版本的冻结事实

---
