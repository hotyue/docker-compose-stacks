# MariaDB runtime env
# 首次安装后会复制到 /opt/docker/mariadb/.env；请按需修改密码

TZ=UTC

# 端口映射（宿主机端口:容器 3306）
MARIADB_PORT=3306

# 必填：root 密码
MARIADB_ROOT_PASSWORD=change_me_strong_root_password

# 可选：初始化库与用户（如果不需要可留空或删除这三行）
MARIADB_DATABASE=app
MARIADB_USER=app
MARIADB_PASSWORD=change_me_strong_app_password
